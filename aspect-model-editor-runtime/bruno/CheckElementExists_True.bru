meta {
  name: CheckElementExists_True
  type: http
  seq: 37
}

get {
  url: http://localhost:{{port}}/ame/api/models/check-element?fileName=OtherFile.ttl
  body: none
  auth: none
}

headers {
  aspect-model-urn: urn:samm:org.eclipse.examples:1.0.0#Movement
  Accept: application/json
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Element exists in different file", function() {
    const data = res.getBody();
    expect(data).to.equal(true);
  });
}
