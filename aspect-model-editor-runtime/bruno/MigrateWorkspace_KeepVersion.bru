meta {
  name: MigrateWorkspace_KeepVersion
  type: http
  seq: 41
}

get {
  url: http://localhost:{{port}}/ame/api/models/migrate-workspace?setNewVersion=false
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Migration result structure", function() {
    const result = res.getBody();
    expect(result).to.have.property('success');
  });
}
